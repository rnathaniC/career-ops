# ── Live auto-submit master switch ────────────────────────────────────────────
# As of 2026-06-18 the per-company allowlist was RETIRED. Live mode now trusts the
# per-card gates that apply to every submission:
#   grade A/B eligibility  ->  readiness band (>=60)  ->  5 submissions/day cap
# Readiness bands:  <60 skip · 60-88 submit WITHOUT cover letter · 89+ submit WITH CL.
#
# enabled: true   -> live auto-submit is ARMED (any qualifying A/B card may submit).
# enabled: false  -> GLOBAL KILL-SWITCH: no live submissions at all. Flip this to
#                    instantly stop everything without touching code or the scheduler.
enabled: true

# (optional) companies: list is no longer enforced. Kept only as a place to jot
# notes; it has no effect on what auto-submits.
companies: []
